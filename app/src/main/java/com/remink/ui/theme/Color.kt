package com.remink.ui.theme

import androidx.compose.ui.graphics.Color

// E-ink palette: high contrast, no mid-tones for rendering clarity
val Black = Color(0xFF000000)
val White = Color(0xFFFFFFFF)
val MidGrey = Color(0xFF808080)

// Disabled state text only — no other uses
val DisabledGrey = Color(0xFF888888)
